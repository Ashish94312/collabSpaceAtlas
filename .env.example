# Django Settings
# Copy this file to .env and update with your values

# Secret Key - Generate a new one for production!
# Generate with: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=django-insecure-$(bjl)gp@=5!so2oj*c$9f5)0h&gr6ep29q0%ifx3inzm!vj6@

# Debug Mode - Set to False in production
DEBUG=True

# Allowed Hosts - Comma-separated list of allowed hosts
# For local development, leave empty or add: localhost,127.0.0.1
ALLOWED_HOSTS=

# Database URL (Optional - only if using PostgreSQL)
# For SQLite (default), leave this empty
# DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# Vercel (Auto-set by Vercel, don't set manually)
# VERCEL=1
